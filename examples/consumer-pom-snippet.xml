<build>
  <plugins>
    <plugin>
      <groupId>com.acme</groupId>
      <artifactId>openshift-db-maven-plugin</artifactId>
      <version>1.0.0</version>
      <executions>
        <execution>
          <id>create-db</id>
          <phase>pre-integration-test</phase>
          <goals><goal>create-db</goal></goals>
          <configuration>
            <namespace>${env.OPENSHIFT_NAMESPACE}</namespace>
            <image>image-registry.openshift-image-registry.svc:5000/${env.OPENSHIFT_NAMESPACE}/postgres-rootless:latest</image>
            <adminUser>postgres</adminUser>
            <adminPassword>postgres</adminPassword>
            <dbUser>testuser</dbUser>
            <dbPassword>testpass</dbPassword>
            <dbPrefix>itdb</dbPrefix>
          </configuration>
        </execution>
        <execution>
          <id>drop-db</id>
          <phase>post-integration-test</phase>
          <goals><goal>drop-db</goal></goals>
          <configuration>
            <namespace>${env.OPENSHIFT_NAMESPACE}</namespace>
          </configuration>
        </execution>
      </executions>
    </plugin>

    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-failsafe-plugin</artifactId>
      <version>3.3.2</version>
      <configuration>
        <systemPropertyVariables>
          <it.db.host>${it.db.host}</it.db.host>
          <it.db.port>${it.db.port}</it.db.port>
          <it.db.name>${it.db.name}</it.db.name>
          <it.db.user>${it.db.user}</it.db.user>
          <it.db.password>${it.db.password}</it.db.password>
        </systemPropertyVariables>
      </configuration>
      <executions>
        <execution>
          <goals>
            <goal>integration-test</goal>
            <goal>verify</goal>
          </goals>
        </execution>
      </executions>
    </plugin>
  </plugins>
</build>